[package]
name = "autonomy-cli"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
autonomy-core = { path = "../autonomy-core" }
autonomy-replay = { path = "../autonomy-replay" }
autonomy-sim = { path = "../autonomy-sim" }
